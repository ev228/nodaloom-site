# Nodaloom — Landing Page

Static landing page for [nodaloom.com](https://nodaloom.com).

## Deploy

This site is deployed on Vercel as a static site. Push to `main` to deploy.

## Structure

```
index.html    — Full landing page (single file, no dependencies)
vercel.json   — Vercel configuration (headers, caching)
```
